export enum ScheduleStatus {
    DEFAULT = 'DEFAULT', // Default schedule status
    MODIFIED = 'MODIFIED', // Schedule has been modified
    SWAPPED = 'SWAPPED', // Schedule has been swapped
    LEAVE = 'LEAVE', // Schedule is a leave day
}